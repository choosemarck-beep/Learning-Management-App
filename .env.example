# Database Connection
# Replace with your Railway PostgreSQL connection string (external/public URL)
# Get this from Railway dashboard → PostgreSQL service → Variables tab → DATABASE_PUBLIC_URL
# Or from Connect tab → Public Network → Connection URL
DATABASE_URL="postgresql://postgres:PASSWORD@HOST.railway.app:PORT/railway"

# NextAuth Secret
# Generate with: openssl rand -base64 32
# This secret encrypts user sessions - keep it secure and never commit to git
NEXTAUTH_SECRET="your-generated-secret-here"

# App URL (for local development)
# Change to your production URL when deploying (e.g., https://your-app.vercel.app)
NEXTAUTH_URL="http://localhost:3000"
